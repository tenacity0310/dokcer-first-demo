name: Notifier

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  messageTelegram:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Telegram
        uses: appleboy/telegram-action@master
        with:
          to: -1002008082378
          token: 6403212406:AAGx9-KiO0kYFSTz0rcwkNXhdbyREr3jq2w
          message: "Hello, this is a test message from GitHub Actions."

